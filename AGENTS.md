# Project Instructions

## Graphify

Use the native `graphify` skill when the user explicitly invokes `/graphify`.

## REPLAY north star

Keep evidence, memory, uncertainty, dispute, and agent inference visibly distinct. UI actions and WebMCP tools must call the same domain command layer. Only a human UI action may confirm a claim or finalize a report.
