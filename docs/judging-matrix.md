# REPLAY judging matrix

Last reconciled with the current deployed source/tests and separately scoped historical/browser evidence: **2026-08-29**. The [official challenge page](https://openai.com/webmcp-challenge/) and [rules](https://webmcp.devpost.com/rules) define four equally weighted criteria, with WebMCP Leverage first in tie-breaking, and a **September 3, 2026 at 1:00 PM PDT** deadline. The final submission must provide a functioning live WebMCP URL, public source/instructions/license, English description, free judge access, and a public YouTube demo under three minutes with audio and actual WebMCP use. This matrix links claims to source, tests, public artifact/browser evidence, and remaining external proof without conflating those categories.

## Judge takeaway

| Criterion             | What REPLAY proves in the first viewing                                                                                                                                                   |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WebMCP Leverage       | A screenshot shows vehicles; typed Site Tools expose their branch-owned timed points, evidence relationships, locks, and current version, then act through the same commands as the UI.   |
| Execution             | One synthetic case moves from calibrated authored motion through provenance/completeness review to a citation-bound report, with every state and consequential decision visible.          |
| Potential Impact      | A driver and claims-intake reviewer turn fragmented incident material into a cited neutral record while evidence, memory, dispute, uncertainty, attestation, and inference stay distinct. |
| Creativity & Ambition | A scene and timeline become a shared human-agent black box: the agent reads, validates, proposes, and drafts; the human attests, decides, and finalizes.                                  |

**Operating contract:** Agent: read, validate, propose, draft. Reviewer: attest, decide, finalize.

Status vocabulary:

- **Implemented in current source:** present in current source/tests, without implying deployment, a native client, or a manual live journey.
- **Deployed + independently payload-verified:** present in commit `b252fbde9551d0a1d2c41a1282ced66dc8ae1b20`, passed CI, and matched its verified build in the automated 2026-08-29 post-deploy byte comparison.
- **Historical public proof:** verified for preceding `cd88755b`, superseded `00688d8a`, or the older `f980d28` baseline; retained as evidence but not attributed to the current release.
- **Specified; run pending:** an eval/check artifact exists without a recorded result.

## 1. WebMCP Leverage

| Evidence                                       | Why it matters                                                                                                                                                                                                                                                                                                                          | Repository proof                                                                                                                                                      | Demo moment                                                               | Status                                                                                                                                                                                         |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current API and progressive enhancement        | Feature-detects `document.modelContext`, registers narrow tools through `modelContext.registerTool(...)`, and retains the complete manual workspace in ordinary browsers.                                                                                                                                                               | `src/webmcp/registry.ts`, `src/react-webmcp.d.ts`, [source-of-truth.md](source-of-truth.md)                                                                           | Header and inspector show Site Tools/manual mode.                         | Current artifact is deployed and byte-verified; bridge evidence remains historical to the preceding payload, while a live supported-model trace remains pending                                |
| Nineteen narrow imperative tools               | Exposes spatial, temporal, provenance, hypothesis, proposal, and report semantics instead of a generic executor. Eighteen exist before preview and 19 while `add_report_note` is eligible; a successful note invalidates the preview and returns the next inventory to 18 until rebuild.                                                | `src/webmcp/tools.ts`, `src/webmcp/schemas.ts`, [webmcp-tools.md](webmcp-tools.md)                                                                                    | Inspect, propose/reconstruct, branch, link evidence, build report.        | Current release deterministic lifecycle/CI and deployment identity verified; live-deployment native supported-model runs pending                                                               |
| Human-gated coordinated proposals              | `propose_scene_changes` writes visible base-versus-proposed deltas for 1–10 distinct actors without applying geometry. It supports complete start-to-final paths and endpoint-preserving interior patches. Only human UI commands adjust/accept/reject; acceptance validates every baseline and lock before all-or-nothing application. | `src/domain/{models,commandSchemas,reducer}.ts`, `src/integration/replayWebMCPAdapter.ts`, `tests/domain/agent-proposals.test.ts`                                     | Agent proposes one or more paths; human reviews and accepts/rejects.      | Current seed-v6 implementation deployed/CI-verified; supported-client run pending                                                                                                              |
| Calibrated validation through WebMCP           | `validate_case_consistency` exposes `all`, `scene`, `timeline`, `geometry`, `motion`, `damage`, `integrity`, `provenance`, `completeness`, and `report`; `scene` composes geometry/motion/damage.                                                                                                                                       | `src/webmcp/{types,schemas,tools}.ts`, `src/domain/{consistency,physics}.ts`, `tests/domain/{consistency,physics,integrity}.test.ts`, `tests/webmcp/registry.test.ts` | Agent explains a footprint-separation or motion advisory and assumptions. | Current seed-v6 calibration scopes deployed/CI-verified; exact-deployment supported-model run pending                                                                                          |
| Shared domain command layer                    | UI and Site Tools use the same validation, authorization, lock, version, idempotency, consistency, and activity boundary.                                                                                                                                                                                                               | `src/components/Workspace.tsx`, `src/integration/replayWebMCPAdapter.ts`, `src/domain/engine.ts`                                                                      | Agent and human edit the same visible scene.                              | Deployed and CI-verified                                                                                                                                                                       |
| Durable versus session audit                   | Domain mutations append canonical persisted activity. Reads, UI-only calls, and rejections produce capped session audit; cancellation before primary persistence produces neither, while failed post-save compensation is session-audited.                                                                                              | `src/webmcp/tools.ts`, `src/components/Workspace.tsx`, adapter/registry tests                                                                                         | Activity rail distinguishes factual change from invocation visibility.    | Implemented in current source                                                                                                                                                                  |
| Explicit human override correlation            | A qualifying human correction retains the agent mutation and links the new `human-override` activity back to it; recent-activity filtering happens before limiting.                                                                                                                                                                     | `src/domain/{models,schema,engine}.ts`, adapter integration tests, critical E2E                                                                                       | Human corrects Vehicle B; agent rereads/revalidates.                      | Implemented in current source; model behavior pending                                                                                                                                          |
| Provenance-rich evidence/report tools          | Annotation links can target claims/events/actors/paths/damage/hypotheses/assumptions; report preview returns missing requirements/unresolved IDs and uses claim/evidence/workspace-path citations.                                                                                                                                      | `src/domain/{models,report,reducer}.ts`, evidence/report tests                                                                                                        | Evidence link and report review beats.                                    | Implemented in current source                                                                                                                                                                  |
| Exact-revision human attestations              | Substantive claim/provenance/link changes, a newly linked evidence item or annotation, or linked/source evidence deletion demote old confirmation and record why; no-op edits preserve it and only the human UI can reconfirm.                                                                                                          | `src/domain/reducer.ts`, `tests/domain/claim-attestation.test.ts`                                                                                                     | Link new evidence; prior confirmation visibly returns to reported.        | Implemented in current source; supported-client journey pending                                                                                                                                |
| Human completeness attestations                | A person can record no evidence supplied, per-actor damage unknown/not assessed, and completed uncertainty review without fabricating a fact. State fingerprints invalidate stale/imported authority; Site Tools can report gaps but cannot attest.                                                                                     | `src/domain/{completeness,reducer,report}.ts`, completeness domain/component/E2E tests                                                                                | Show the report's cited **Human attestation** records.                    | Deployed and CI-verified; current supported-client journey pending                                                                                                                             |
| Correct hints and visible UI writes            | Reads use read-only hints; `compare_hypotheses` and preview are declared UI writes and visibly open their views. All imperative outputs are labelled untrusted because compact results can contain case-derived text/metadata.                                                                                                          | `src/webmcp/tools.ts`, `tests/webmcp/registry.test.ts`                                                                                                                | Inspector annotation chips and comparison/report navigation.              | Implemented in current source                                                                                                                                                                  |
| Versioning, locks, idempotency, cancellation   | Stale/repeated/locked work cannot silently overwrite newer state. Request IDs bind to semantic intent; WebMCP stages and CAS-saves before live commit, compensates a resolved save on later abort/conflict, and surfaces failed compensation.                                                                                           | `src/domain/engine.ts`, `src/domain/reducer.ts`, real-adapter/registry/domain tests                                                                                   | Human correction and adversarial checks.                                  | Implemented in current source; actual-Dexie/browser edge pending                                                                                                                               |
| Declarative human-only finalization            | `finalize_factual_report` has no `toolautosubmit`; four fingerprint-bound acknowledgements—including every labelled unconfirmed or hypothesis statement—and a second human confirmation create the snapshot. OpenAI Site Tools currently does not expose the declarative form; ordinary browser interaction is not a WebMCP call.       | `src/components/InspectorPanel.tsx`, report commands/tests, [source-of-truth.md](source-of-truth.md)                                                                  | Final report beat.                                                        | Implemented in current source; compatible-Chrome native lifecycle pending                                                                                                                      |
| Deterministic tests plus twelve eval scenarios | Separates code invariants from probabilistic tool choice and covers complete start-to-final proposals, positive inference provenance, injection, locks, stale versions, cancellation, human-only decisions, and current realism/integrity rules.                                                                                        | `tests/`, `evals/webmcp-evals.json`, [webmcp-evals.md](webmcp-evals.md)                                                                                               | Submission evidence.                                                      | `b252fbd` CI passed 460/460 Vitest and 230 Playwright runs (221 passed, 9 intentional skips, 0 failed), plus 12/12 configured-base focused runs; exact-deployment supported-model runs pending |

Current public proof is bounded to the application payload from commit `b252fbde9551d0a1d2c41a1282ced66dc8ae1b20`: clean CI passed 460/460 Vitest tests, 230 Playwright runs, and 12/12 configured-base focused runs; the live evidence endpoint and verify-deployment gate byte-matched all 46 public payload files / 5,297,092 bytes, yielding manifest SHA-256 `22c26f2b61944986272a28d7568fd1421b96b62d37e07dec60fd34895f2aa9c9`. Deterministic registry/adapter tests prove full one-actor start-to-final proposal preview and human-only acceptance; a separate local stress run covered the playback boundary with 50/50 exact-impact checks plus 20/20 mixed critical journeys. A cache-busted live operator smoke then confirmed both one-click impact continuation paths and visibly inspected the published single-change/full-path proposal schema with no console errors or failed dynamic requests. This is current product/contract evidence, not native Site Tools invocation or supported-model choice. The preceding `cd88755b` operator bridge/Lighthouse checks, the seed-v3 `00688d8a` browser evidence, and the older `f980d28` native proof remain historical.

The exact release artifacts remain separately scoped by base path. On Node 22.13.0, the root artifact has 46 public payload files / 5,296,840 bytes plus `.nojekyll`, manifest SHA-256 `efcc58d83a224460c2d53ebd1c14786095f91cd07a2f17c8b22c13afcbbeced0`; the deployed `/replay/` artifact has 46 public payload files / 5,297,092 bytes plus `.nojekyll`, manifest SHA-256 `22c26f2b61944986272a28d7568fd1421b96b62d37e07dec60fd34895f2aa9c9`. The exact configured-base artifact passed 12/12 automated focused runs, and its complete eight-section handler response was 18,970 bytes against a 32,768-byte compact target and 524,288-byte hard cap. Lighthouse and operator-directed in-app-browser results remain historical to `cd88755b`. Supported-model choice, **Recently used/Sources**, a current live mutation/lifecycle, and cross-client results remain pending.

## 2. Execution

| Evidence                                | Why it matters                                                                                                                                                                                                                                      | Repository proof                                                                                                                                           | Demo moment                                                                         | Status                                                                                                                                        |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Complete start-to-export workflow       | Landing, guide/tour, demo/wizard, rotation and timed path editing, facts/evidence/questions/branches/proposals, report review/finalization, and downloads form one workflow.                                                                        | `src/components/`, `src/export/`, E2E tests                                                                                                                | Entire recording.                                                                   | Implemented in current source                                                                                                                 |
| Stable local cases and exact contacts   | Fresh runs receive stable `#case/<encoded-case-id>` routes and remain discoverable from the landing page; multi-vehicle authoring targets one exact actor pair without overwriting other contacts.                                                  | `src/App.tsx`, landing/scene components, navigation and multi-vehicle impact E2E tests                                                                     | Reopen a retained run; place one pair-specific contact.                             | Implemented in current source                                                                                                                 |
| Schema-v2 persistence and recovery      | v1 migration, separate v2/legacy vaults, strict new writes, CAS, best-effort edit lease, cross-tab pause, retained raw recovery, narrow audited legacy-index repair, and runtime blob integrity checks reduce silent loss/corruption.               | `src/persistence/database.ts`, `src/domain/importExport.ts`; persistence tests cover case/blob round-trip, migration, strict boundaries, recovery, and CAS | Resume/reset/recovery can be documented.                                            | Implemented in current source; current public multi-tab/migration smoke pending                                                               |
| Honest structured transfer              | JSON excludes evidence bytes and contains the source case ID; a cancel-first UI review itemizes unavailable bytes and each unsigned trust reset before import creates a re-keyed local copy.                                                        | `src/domain/importExport.ts`, import dialog/E2E tests, [data-model.md](data-model.md)                                                                      | Review the transfer summary before opening it.                                      | Implemented in current source                                                                                                                 |
| Deterministic consistency and reporting | Timeline, calibrated geometry, motion-envelope, exact branch/provenance indexes, damage, integrity, completeness, and report issues are projections over declared inputs, not forensic/truth/fault decisions.                                       | `src/domain/{consistency,physics,report,importExport}.ts`, focused tests                                                                                   | Inspect, revalidate, report.                                                        | Current seed-v6 implementation deployed/CI-verified; model trace pending                                                                      |
| Visible post-contact authored response  | Both demo paths slow and diverge after the reported contact; the impact inspector compares incoming/outgoing authored legs without presenting a generated collision response or causal conclusion.                                                  | Demo fixture, impact inspector, physics/domain tests, [demo-script.md](demo-script.md)                                                                     | Play motion around impact and show before → after readout.                          | Current authored contact response deployed/CI-verified; final live recording pending                                                          |
| Five templates and four scenarios       | Calibrated roundabout/intersection/T-junction/straight-road/parking templates and four stable synthetic accounts make realistic and adversarial review repeatable without claiming universal realism.                                               | `src/domain/{roadTemplates,demoScenarios}.ts`, `tests/domain/{demoScenarios,consistency,physics}.test.ts`                                                  | Contrast path-derived high-speed review with the low-speed parking record conflict. | Current seed-v6 scenarios deployed and CI-verified; exact-current multi-scenario live run pending                                             |
| Accessibility and security regressions  | Keyboard/exact editors and annotation targets, semantic tabs, dialog focus/Escape/restoration/scroll lock, 320px/200%-text reflow, mobile guide/tour reachability, axe states, explicit export checks, and iframe blocking have automated coverage. | `tests/e2e/`, component tests                                                                                                                              | Keyboard correction and review.                                                     | Current-release 230-run E2E and 20-baseline visual gate passed; manual assistive-technology and real-device review remains                    |
| Five active synthetic assets            | Versioned, checksum-documented images support a coherent demo while functional geometry remains semantic SVG.                                                                                                                                       | [generated-assets.md](generated-assets.md), `public/assets/generated/`                                                                                     | Landing/evidence views.                                                             | All active/compatibility assets are public and matched the artifact in the independent comparison                                             |
| Public build evidence                   | Judges need a stable HTTPS URL matching source, video, and documentation.                                                                                                                                                                           | [deployment.md](deployment.md), [testing.md](testing.md); [test-report.md](test-report.md) is explicitly historical                                        | Every live beat.                                                                    | `b252fbd` deployed; automated verification matched all 46 files / 5,297,092 bytes; Lighthouse evidence remains historical to earlier payloads |

## 3. Potential Impact

| Evidence                                       | Why it matters                                                                                                                                                                                                                                          | Repository proof                                                                            | Demo moment                                     | Status                                                      |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------- |
| Specific bounded audience                      | A driver documenting a minor no-injury incident and a claims-intake reviewer get a neutral record; fleet, rental-support, and mediation teams are adjacent users, not a claim of universal scope.                                                       | `PRODUCT.md`, README, landing copy                                                          | Opening narration.                              | Implemented in current source                               |
| Provenance and uncertainty                     | Source-required photo/document observations, certainty, removable relationships that preserve evidence assets, exact-revision attestations, author/origin, override links, questions, and branches prevent inference from silently hardening into fact. | `src/domain/models.ts`, `src/domain/reducer.ts`, inspector/activity UI                      | Inspect, correct a link, and review provenance. | Implemented in current source                               |
| Visible review remains decisive                | Agents organize, propose, and report gaps; no Site Tool can attest/reconfirm completeness or claims, decide proposals, delete evidence, or finalize. Those commands require the visible UI review origin.                                               | Domain actor/origin authorization and visible controls                                      | Proposal and finalization beats.                | Implemented in current source                               |
| Local-first with an explicit external boundary | Manual data is origin-local; Site Tools can send compact structured results to its client/model service but never evidence bytes. Shared GitHub Pages origin is disclosed.                                                                              | `PRIVACY.md`, `SECURITY.md`, [security-and-privacy-notes.md](security-and-privacy-notes.md) | Closing privacy note.                           | Implemented disclosure; dedicated-origin deployment pending |

## 4. Creativity and Ambition

| Evidence                                              | Why it matters                                                                                                                                                                                   | Repository proof                              | Demo moment                        | Status                        |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------- | ---------------------------------- | ----------------------------- |
| Scene and timeline as collaboration surface           | Human and agent operate on calibrated spatial/temporal state rather than a hidden chat transcript.                                                                                               | scene/timeline components and scene tools     | Reconstruction/playback.           | Implemented in current source |
| Realism plus adversarial scenario lab                 | High-speed values remain visibly path-derived and ask for telemetry; a low-speed stationary-vs-timestamped-motion conflict tests review without becoming a measurement, lie, or intent detector. | physics/scenario/consistency domain and tests | High-speed/parking contrast.       | Implemented in current source |
| Proposal-before-application interaction               | One- or multi-actor geometry work becomes an inspectable reversible object with immutable revisions and a human decision.                                                                        | proposal domain/UI/tool                       | Coordinated reconstruction review. | Implemented in current source |
| Bidirectional correction with explicit history        | Humans directly correct agent geometry; both the original and override remain attributable for later agent context.                                                                              | activity/override implementation and tests    | Human correction/revalidation.     | Implemented in current source |
| Branch-local uncertainty and evidence-bound reporting | Competing geometry stays explicit while shared facts retain identity; report output preserves certainty, citations, limitations, and human review.                                               | hypotheses/report domain and UI               | Comparison and report.             | Implemented in current source |

## Submission blockers to close

1. Repeat and expand the current `b252fbde` public smoke across all four scenarios, the native 18→19 lifecycle, and fresh-profile persistence; do not reuse the historical `cd88755b` bridge/high-speed trace as current evidence.
2. Run all eleven supported-model eval scenarios in each native Site Tools model/client and retain tool traces plus every safety failure; run eval 10 separately in its deterministic harness.
3. Verify the current imperative lifecycle natively and the declarative lifecycle in compatible Chrome.
4. Complete physical screen-reader, shipping Safari/real-device, broader pointer/multi-touch/file-picker, raw-recovery and cross-profile/multi-tab stress, and platform-native downloaded-file inspection. Retain the automated upload/delete/reload, conflict, focus, reflow, and export checks as regression evidence rather than treating them as substitutes for the manual matrix.
5. Deploy to a dedicated header-capable origin if production-like privacy/security claims are required; GitHub Pages is a shared-origin synthetic demo.
6. Record/publish the under-three-minute public YouTube demo with audio and actual WebMCP use from an explicit fresh deterministic run, then replace the placeholder.
