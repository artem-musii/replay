# REPLAY judging matrix

Last reconciled with the implementation: **2026-08-27**. The [WebMCP Challenge](https://webmcp.devpost.com/) and [official rules](https://webmcp.devpost.com/rules) define four equally weighted criteria, with WebMCP Leverage first in tie-breaking. This matrix links every submission claim to inspectable repository evidence and marks remaining external verification honestly. API claims follow the current [WebMCP draft](https://webmachinelearning.github.io/webmcp/), [Chrome documentation](https://developer.chrome.com/docs/ai/webmcp), and [OpenAI Site Tools documentation](https://learn.chatgpt.com/docs/webmcp) recorded in the repository source of truth.

Status vocabulary:

- **Implemented:** present in source and available in the local product.
- **Implemented; browser verification pending:** source and deterministic coverage exist, but the supported live browser/agent path still needs a captured run.
- **Specified; run pending:** the evaluation artifact exists, but no result is claimed.
- **Pending external release step:** requires deployment, publishing, or a public account action not completed in this workspace.

## 1. WebMCP Leverage

| Evidence                                              | Why it matters                                                                                                                                                                                   | Repository proof                                                                                                                                   | Demo moment                                                              | Status                                                           |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------- |
| Current API and feature detection                     | Uses `document.modelContext.registerTool(...)` without depending on obsolete navigator APIs; ordinary browsers do not crash.                                                                     | `src/webmcp/registry.ts`, `src/react-webmcp.d.ts`, [source-of-truth.md](source-of-truth.md)                                                        | Header/inspector shows Site Tools or manual mode.                        | Implemented; browser verification pending                        |
| 18 narrow imperative tools                            | The agent reads and edits spatial, temporal, provenance, hypothesis, and report state through task-specific semantics rather than a generic command executor.                                    | `src/webmcp/tools.ts`, `src/webmcp/schemas.ts`, [webmcp-tools.md](webmcp-tools.md)                                                                 | Inspect, reconstruct, branch, link evidence, build report.               | Implemented; browser verification pending                        |
| Meaningful lifecycle groups                           | Base, scene, and facts tools follow case availability; the hypothesis group includes preview construction after a baseline; report notes join only after a preview. Abort tears down each group. | `src/webmcp/registry.ts`, `src/webmcp/tools.ts`, `tests/webmcp/registry.test.ts`                                                                   | WebMCP inspector shows lifecycle mode and registration states.           | Implemented; browser verification pending                        |
| Read-only and untrusted-output hints                  | Clients can distinguish mutation-free reads and outputs containing user/evidence content. Application validation remains authoritative.                                                          | `src/webmcp/tools.ts`, `tests/webmcp/registry.test.ts`                                                                                             | Inspector annotation chips.                                              | Implemented                                                      |
| Invocation cancellation                               | Each execution receives a signal, stages/checks work, and clears transient agent-working state in `finally`; registration signals remain separate.                                               | `src/webmcp/tools.ts`, `src/webmcp/registry.ts`, `src/domain/engine.ts`                                                                            | Cancel test/debug path, not a primary demo beat.                         | Implemented; supported-browser edge verification pending         |
| Shared domain command layer                           | Human handlers and WebMCP mutations both enter the same validated engine, so permissions and consistency rules cannot diverge by surface.                                                        | `src/components/Workspace.tsx`, `src/integration/replayWebMCPAdapter.ts`, `src/domain/engine.ts`, `src/domain/commandSchemas.ts`                   | Agent path appears in the same scene/timeline/activity the person edits. | Implemented                                                      |
| Versioning, idempotency, locks, and reversible action | Stale or repeated calls cannot silently overwrite newer work; locked items and unsafe reverts produce structured failures.                                                                       | `src/domain/engine.ts`, `src/domain/reducer.ts`, `tests/domain/engine.test.ts`                                                                     | Human correction is preserved before revalidation.                       | Implemented                                                      |
| Visible tool activity and affected-item reveal        | Agent work has a live header state, durable activity attribution, and brief affected-item highlighting.                                                                                          | `src/components/Workspace.tsx`, `src/components/ActivityPanel.tsx`, `src/webmcp/tools.ts`                                                          | Every agent mutation visibly changes the page and activity rail.         | Implemented; agent-client run pending                            |
| Human override detection                              | `get_recent_activity` exposes bounded attributed history so an agent can see a newer human scene correction.                                                                                     | `src/webmcp/tools.ts`, `src/domain/selectors.ts`, `src/components/Workspace.tsx`                                                                   | Drag/rotate Vehicle B, then ask the agent to revalidate.                 | Implemented; agent-behavior eval pending                         |
| Human-only claim confirmation                         | Agent input schemas exclude confirmed status and the domain engine rejects agent-origin confirmation even if forced.                                                                             | `src/webmcp/schemas.ts`, `src/domain/commandSchemas.ts`, `src/domain/engine.ts`, `tests/domain/engine.test.ts`                                     | Facts tab labels explicit human confirmation.                            | Implemented                                                      |
| Declarative, human-gated finalization                 | The visible `finalize_factual_report` form has `toolname`/`tooldescription` and no automatic submission. Three acknowledgements plus a manual confirmation click create the snapshot.            | `src/components/InspectorPanel.tsx`, `src/domain/commandSchemas.ts`, `src/domain/engine.ts`                                                        | Final 25 seconds of the demo.                                            | Implemented; declarative activation in supported browser pending |
| Prompt-injection boundary                             | User/evidence text is treated as untrusted data, tool names/descriptions are fixed, and there is no arbitrary executor or automatic external-send/delete tool.                                   | `src/webmcp/tools.ts`, `src/webmcp/schemas.ts`, [security-and-privacy-notes.md](security-and-privacy-notes.md), [webmcp-evals.md](webmcp-evals.md) | Mention during evidence provenance beat.                                 | Implemented; probabilistic injection run pending                 |
| Normal-browser progressive enhancement                | The entire workspace remains operable when `document.modelContext` is missing.                                                                                                                   | `src/App.tsx`, `src/components/Workspace.tsx`, `src/webmcp/registry.ts`                                                                            | Header shows manual mode; all visible controls remain enabled.           | Implemented; cross-browser manual pass pending                   |
| Deterministic WebMCP tests and model evals            | Registry logic and direct safety invariants can be tested separately from probabilistic tool selection.                                                                                          | `tests/webmcp/registry.test.ts`, `evals/webmcp-evals.json`, [webmcp-evals.md](webmcp-evals.md)                                                     | Submission evidence rather than narrated demo.                           | Deterministic tests implemented; model runs pending              |

## 2. Execution

| Evidence                                      | Why it matters                                                                                                                                                                              | Repository proof                                                                                          | Demo moment                                     | Status                                                            |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------- |
| Complete start journey                        | Concise landing, browser status, deterministic demo action, and a three-step blank-case wizard work without an account.                                                                     | `src/components/LandingPage.tsx`, `src/components/BlankCaseWizard.tsx`, `src/App.tsx`                     | Video starts in demo; README covers blank flow. | Implemented                                                       |
| Polished seeded demo                          | Roundabout case has two vehicles, wet conditions, claims, four synthetic images, damage, final positions, open questions, and a deliberate geometry inconsistency with no fault conclusion. | `src/domain/seed.ts`, `public/assets/generated/`                                                          | Entire recording.                               | Implemented                                                       |
| SVG scene and synchronized timeline           | Scene geometry stays crisp and selectable; playback and editing use deterministic interpolation and branch scope.                                                                           | `src/components/SceneCanvas.tsx`, `src/components/Timeline.tsx`, `src/domain/interpolation.ts`            | Reconstruction, human override, comparison.     | Implemented                                                       |
| Facts, evidence, questions, and hypotheses    | The inspector supports the complete documentation workflow and keeps state categories distinct.                                                                                             | `src/components/InspectorPanel.tsx`, `src/domain/models.ts`, `src/domain/reducer.ts`                      | Evidence/provenance and branch beats.           | Implemented                                                       |
| Deterministic consistency checks              | Geometry, timeline, provenance, completeness, and report rules produce inspectable issues without asking a model to determine truth or fault.                                               | `src/domain/consistency.ts`, `tests/domain/interpolation-consistency.test.ts`                             | First inspect and post-correction revalidation. | Implemented                                                       |
| Persistence and recovery                      | Cases and evidence blobs use IndexedDB; deterministic reset, JSON import/export, schema checks, and referential validation protect local recovery.                                          | `src/persistence/database.ts`, `src/domain/importExport.ts`, `src/App.tsx`                                | Reset/export can be shown or documented.        | Implemented; full reload/browser matrix pending                   |
| Undo, redo, and agent reversion               | The human can recover from normal edits; command history stops at lock/snapshot safety boundaries.                                                                                          | `src/domain/engine.ts`, `src/components/Workspace.tsx`, `tests/domain/engine.test.ts`                     | Header controls and activity actions.           | Implemented                                                       |
| Evidence-bound report and four export formats | Preview separates certainty classes and citations; human finalization creates a snapshot; PDF, JSON, SVG, and PNG exports are local.                                                        | `src/domain/report.ts`, `src/export/exporters.ts`, `src/components/InspectorPanel.tsx`                    | Report/finalization/export beat.                | Implemented; final cross-browser download pass pending            |
| Accessibility foundations                     | Semantic buttons/forms/dialogs, skip link, keyboard scene/timeline controls, focus styling, non-color labels, live regions, and reduced-motion CSS are present.                             | `src/components/`, `src/styles/`, `tests/components/timeline.test.tsx`, `tests/e2e/accessibility.spec.ts` | Keyboard human correction can be shown.         | Automated axe guardrails pass; screen-reader review pending       |
| Responsive product design                     | Editorial landing, focused desktop notebook workspace, compact breakpoints, and mobile review behavior use generated and SVG assets without generic dashboard styling.                      | `src/styles/`, `DESIGN.md`, `public/assets/generated/`                                                    | Visible throughout.                             | Implemented; visual regression matrix pending                     |
| Automated verification                        | Lint, strict TypeScript, 53 Vitest tests, 28 desktop/mobile Playwright runs, four-state axe checks, and the production build passed at the 2026-08-27 documentation snapshot.               | `package.json`, `tests/`, [testing.md](testing.md)                                                        | Submission documentation.                       | Local automated gate implemented; live/model/manual gates pending |
| Live HTTPS deployment                         | Judges need a stable free URL with correct WebMCP/security headers and assets.                                                                                                              | [deployment.md](deployment.md)                                                                            | Required for every live demo beat.              | Pending external release step                                     |

## 3. Potential Impact

| Evidence                                    | Why it matters                                                                                                                                       | Repository proof                                                                                | Demo moment                               | Status                             |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------- | ---------------------------------- |
| Specific audience and narrow incident scope | Drivers and incident-support teams have a believable, bounded workflow instead of a generic forensic platform.                                       | `PRODUCT.md`, README, landing copy                                                              | Opening narration.                        | Implemented                        |
| Fragmented incident-data problem            | Scene, time, photos, damage, statements, and unanswered details remain connected rather than flattened into one form paragraph.                      | `src/domain/models.ts`, scene/timeline/inspector UI                                             | Whole demo.                               | Implemented                        |
| Provenance and uncertainty                  | Certainty status, source type, evidence links, author/origin, branch scope, and change history remain explicit.                                      | `src/domain/models.ts`, `src/components/InspectorPanel.tsx`, `src/components/ActivityPanel.tsx` | Inspect and evidence beats.               | Implemented                        |
| Report completeness without overreach       | Missing details and unresolved questions remain visible; confirmed report statements require human-confirmed source claims; hypotheses stay labeled. | `src/domain/consistency.ts`, `src/domain/report.ts`                                             | Report preview.                           | Implemented                        |
| Local-first privacy                         | No account or backend is required; case/evidence data remains in browser storage unless explicitly exported.                                         | `src/persistence/database.ts`, [security-and-privacy-notes.md](security-and-privacy-notes.md)   | Privacy can be a short closing-card note. | Implemented; network audit pending |
| Safety constraints                          | Product language and domain rules reject fault/liability determination, automatic confirmation/finalization, and forensic claims.                    | `src/domain/engine.ts`, `src/domain/consistency.ts`, report disclaimer, README                  | Present in facts/report UI.               | Implemented                        |

## 4. Creativity and Ambition

| Evidence                                                       | Why it matters                                                                                                                               | Repository proof                                                                               | Demo moment                      | Status                              |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------- | ----------------------------------- |
| Spatial and temporal state as the shared collaboration surface | The scene and timeline, not a transcript, are the primary object both parties inspect and edit.                                              | `src/components/SceneCanvas.tsx`, `src/components/Timeline.tsx`, WebMCP scene tools            | Reconstruction and playback.     | Implemented                         |
| Visible bidirectional correction                               | Agent-created geometry is directly editable by the person; newer attributed activity gives the agent correction context.                     | `src/components/Workspace.tsx`, `src/integration/replayWebMCPAdapter.ts`, recent-activity tool | Human override and revalidation. | Implemented; live agent run pending |
| Provenance as product interaction                              | Certainty and sources affect what can be confirmed, shared across branches, cited, or finalized instead of appearing as decorative metadata. | `src/domain/reducer.ts`, `src/domain/report.ts`, inspector UI                                  | Facts/evidence/report beats.     | Implemented                         |
| Human-only consequential state                                 | Agent contribution is useful without allowing agent inference to harden invisibly into fact or a final report.                               | Domain authorization plus declarative form                                                     | Human finalization.              | Implemented                         |
| Alternative hypotheses as branch-local geometry                | Competing explanations differ in explicit assumptions, paths, timing, evidence support, and issues while shared facts keep stable identity.  | `src/domain/hypotheses.ts`, `src/domain/reducer.ts`, scene/timeline comparison                 | Hypothesis comparison.           | Implemented                         |
| Evidence-bound neutral reporting                               | Report generation is a deterministic cited projection, with agent notes kept unreviewed until a person approves them.                        | `src/domain/report.ts`, report UI, report tests                                                | Final report beat.               | Implemented                         |

## Submission blockers to close

1. Publish the repository with the MIT license and replace every placeholder URL.
2. Deploy the exact final commit over HTTPS and verify the required response headers.
3. Run the final clean-checkout command sequence and complete the remaining manual screen-reader, export-opening, persistence, responsive, performance, and network/console audits.
4. Capture supported Chrome and ChatGPT/Codex Site Tools runs, including the ten-eval matrix; report failures rather than averaging away safety issues.
5. Record and publish the under-three-minute YouTube demo from a deterministic reset.
6. Reconcile this matrix, README, Devpost text, implementation status, and test report with the shipped commit.
